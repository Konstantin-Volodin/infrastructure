# secrets
.secrets
.env

# only reachable if a storage root is pointed back inside the repo — the
# defaults live in /srv/void, and nothing the stack runs writes here
data/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# misc
images/
services/recipe-ocr/
.claude
CLAUDE.md
branding/