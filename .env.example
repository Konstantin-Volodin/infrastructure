PIHOLE_TIMEZONE=America/Montreal

# Authentik
AUTHENTIK_POSTGRES_PASSWORD=    # strong random password
AUTHENTIK_SECRET_KEY=           # generate with: openssl rand 60 | base64 -w 0

# Immich
UPLOAD_LOCATION=./library             # where photos are stored on host
IMMICH_DB_DATA_LOCATION=./postgres    # where the database is stored on host
IMMICH_VERSION=release
IMMICH_DB_PASSWORD=                   # strong random password
IMMICH_DB_USER=postgres
IMMICH_DB_NAME=immich

# Mealie
MEALIE_TIMEZONE=America/Montreal
MEALIE_BASE_URL=http://<tailscale-ip>:9925
