---
###############################################################
#                         Users Database                      #
###############################################################

# Seed template. scripts/env.sh copies this to $CONFIG_DIR/authelia/ on first
# run and hashes a password into the copy. Authelia owns it from then on —
# edits here only affect a fresh install.

# List of users
users:
  vox:
    disabled: false
    displayname: 'vox'
    # hash is written by scripts/env.sh on first run
    password:
    email: 'volodin.kostia@gmail.com'
    groups:
      - 'admins'
      - 'dev'